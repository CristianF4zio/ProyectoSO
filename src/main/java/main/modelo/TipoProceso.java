package main.modelo;

/**
 * Enum que representa los tipos de proceso según su comportamiento
 */
public enum TipoProceso {
    CPU_BOUND, // Proceso intensivo en CPU (muchos cálculos)
    IO_BOUND // Proceso intensivo en I/O (muchas operaciones de entrada/salida)
}
