build.generated.sources.dir=${build.dir}/generated-sources
debug.modulepath=\
    ${run.modulepath}
debug.test.modulepath=\
    ${run.test.modulepath}
excludes=
includes=**
javac.modulepath=
javac.processormodulepath=
javac.test.modulepath=\
    ${javac.modulepath}
# NetBeans Project Properties
project.name=ProyectoSO1
project.type=java
project.encoding=UTF-8

# Main class configuration
main.class=main.DemoSimulador

run.modulepath=\
    ${javac.modulepath}
run.test.modulepath=\
    ${javac.test.modulepath}
# Source directories
src.dir=src/main/java
test.src.dir=src/test/java

# Build directories
build.dir=build
dist.dir=dist
build.classes.dir=${build.dir}
build.test.classes.dir=${build.dir}

# Classpath configuration
run.classpath=${build.classes.dir}
javac.classpath=

# Compiler settings
javac.source=11
javac.target=11
javac.compilerargs=-release 11 -Xlint:unchecked -Xlint:deprecation

# Classpath
javac.classpath=
javac.test.classpath=${javac.classpath}:${build.test.classes.dir}

# JVM arguments
run.jvmargs=
run.args=

# Execution settings
run.classpathmode=classpath
run.test.classpathmode=classpath
